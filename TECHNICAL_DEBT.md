# Technical Debt Register

**Purpose:** a brutally honest, prioritized list of every known debt item. Add an entry the moment debt is knowingly introduced. Close entries as fixed — don't delete the row, mark it resolved with a date, so the history stays visible.

**Audience:** engineers, especially before starting new feature work — check this list first.

**Severity scale:** 🔴 Critical (blocks the product's core promise) · 🟠 High (actively causing or will soon cause real problems) · 🟡 Medium (annoying, not urgent) · 🟢 Low (cosmetic/style)

---

| ID | Item | Severity | Why it exists | Impact | Suggested fix | Est. effort |
|---|---|---|---|---|---|---|
| TD-001 | Fundamentals data is a frozen one-time Kaggle CSV snapshot with no refresh mechanism and no staleness indicator in the UI | 🔴 | Live fundamentals sourcing (NSE filings or a paid API) is genuinely hard; the Kaggle export was the fast path to get the schema populated | Every fundamental-side score in the product may be silently arbitrarily stale, and nothing tells the user | Short-term: add a visible "as of" date wherever a score is shown. Long-term: replace with a refreshable source | Short-term: 0.5–1 day. Long-term: 1–2 weeks |
| TD-002 | Duplicate frontend directory trees (`components/` vs `shared/components/`, `hooks/` vs `features/*/hooks/`, `lib/api/` vs `features/*/api/`) confirmed dead, deleted twice, **present again a third time as of Milestone 3 — reopened, not re-deleted this time** | 🟠 | Mid-migration to the `features/*` structure was never finished by deleting the old trees; deleting them per-milestone hasn't stuck, which points at a packaging/handoff problem rather than a code problem | Confusing for any future contributor; already caused one broken-import bug (documented in historical `CHANGELOG.md` entries) | **Milestone 1:** re-verified zero live imports, deleted `src/components/`, `src/hooks/`, `src/lib/api/`, plus orphaned `src/lib/mock-data.ts`/`src/lib/utils.ts`. **Milestone 2:** found present again, re-verified dead, deleted again at founder's request. **Milestone 3:** found present again a third time, re-verified dead by the same method (zero live imports, `components.json` aliases unchanged) — **left in place this time per founder's explicit instruction**, since a third recurrence is a signal about the delivery/handoff/packaging process, not something a fourth silent deletion would actually fix. Needs its own investigation: is each session starting from a stale base snapshot? Is there an export/zip step that doesn't reflect working-tree deletions? | 0.5 day to delete again; investigation effort for the root cause is unscoped |
| TD-003 | ~~Journal has a full DB schema but zero backend routes/services — frontend runs on static mock data with no persistence~~ **RESOLVED Milestone 2** | 🔴 | Backend write endpoints were never built for this module; frontend was built ahead of the API | The product's stated "emotional core" feature cannot be used by the actual user at all today | **Done:** built `routes/journal.py` + `services/journal_service.py` + `schemas/journal.py` (full CRUD for `journal_entries`: create/read/update/delete, validation, 400/404 handling, symbol FK check, auto-computed `reviewDueAt`). Frontend `/journal` now reads and writes through this API — mock data removed. `journal_reviews` (the review/retrospective table) is explicitly **not** covered — see new TD-017 | 3–5 days |
| TD-004 | ~~Pipeline (`pipeline_items`) has a read-only API — no way to add/move/remove an item via the app~~ **RESOLVED Milestone 3** | 🟠 | Same pattern as TD-003, smaller scope | Watchlist stages are decorative until this is closed | **Done:** built `routes/pipeline.py` + `services/pipeline_service.py` + `schemas/pipeline.py` (full CRUD + dedicated stage-move for `pipeline_items`: create/read/update/delete/move, validation, 400/404 handling, symbol FK check). Frontend `/ideas` now reads and writes through this API — the grouped `GET /pipeline` stays as the read path, only gained an additive `id` field per item. Stages intentionally unchanged (still `Watching`/`Researching`/`Conviction`); no drag-and-drop added, moves go through a dropdown action menu instead | 1–2 days |
| TD-005 | No test suite anywhere (`tests/.gitkeep` only) across 7 backend modules and all frontend code | 🟠 | Solo project, fast iteration prioritized over test coverage so far | Every change to `scoring_service.py`, ingest scripts, or shared components is a manual "did I break the homepage" check; risk compounds with every added module | Start with pure-function unit tests for `scoring_service.py` and `analysis/rules/*` (cheapest, highest-value target — no DB/network mocking needed) | 2–3 days for a meaningful starting suite |
| TD-006 | Backend deployment target undocumented; no Dockerfile/deploy config found for the FastAPI app | 🟡 | Frontend deployment (Cloudflare Workers) was set up; backend's equivalent wasn't documented alongside it | Anyone other than the founder can't stand up the full stack from the repo alone | Document current backend hosting (if any) or add a minimal deploy config (Dockerfile + one-line deploy instructions) | 0.5–1 day |
| TD-007 | Frontend deployed with SSR to Cloudflare Workers for a single-user product | 🟡 | Likely inherited from the scaffolding tool/template used to start the frontend, not a deliberate scaling decision | Added operational complexity (edge runtime debugging, SSR-specific bugs) with no corresponding user-facing benefit yet | Move to a static SPA build against the same API until multi-user need is real; revisit SSR later if it becomes relevant | 1–2 days |
| TD-008 | Weekly Market Intelligence (news) pipeline has never run against a live feed; validated only with synthetic data | 🟠 | Build environment had no outbound network access | Feature reads as "done" (code exists, module report written) but is functionally unverified — risk of shipping a silently-broken feature | Run manually against real RSS feeds once network access is available; verify sector classification and summaries against real articles before enabling in the scheduled cron | 1–2 days |
| TD-009 | News/weekly-intelligence refresh job not present in the scheduled GitHub Actions workflow even though a CLI entrypoint exists (`ingest/weekly_news_refresh.py`) | 🟡 | Likely deferred pending TD-008's validation | Even a working pipeline currently requires manual triggering | Add to `.github/workflows/ingest.yml` on a weekly schedule, but only after TD-008 is resolved | 0.5 day |
| TD-010 | `financials_quarterly` modeled as a time series but every consumer reads only `order by updated_at desc limit 1` | 🟡 | No feature has needed trend data yet | Blocks the earnings-quality and multi-quarter-trend scoring factors in `SCORING_ENGINE.md` v2 without additional work later | No fix needed until a v2 scoring factor requires it — tracked here so it isn't forgotten, not because it's broken today | N/A until needed |
| TD-011 | `shareholding_pattern` (institutional interest data) exists and is fully populated but is not referenced anywhere in scoring logic | 🟡 | Scored engine v1 shipped before this table was wired in | Cheapest available scoring improvement is sitting unused | Wire FII/DII trend and pledge% into `compute_scores.py`'s `fundamental_score` | 1 day |
| TD-012 | ~~`CLAUDE.MD` describes the product as an "institutional-grade... Bloomberg Terminal"-scale platform, contradicting `Product_Vision.md` and `PRODUCT_REQUIREMENTS.md`~~ **RESOLVED Milestone 1** | 🟠 | Written at a different point in the project's evolution, never reconciled with the narrower personal-tool vision that followed | Any AI coding assistant or new contributor reading `CLAUDE.MD` first would over-scope new work | **Done:** founder confirmed `CLAUDE.MD` was intentionally removed from the repository (not rewritten). Its still-valid visual/style guidance (dark-first, dense, premium typography) is preserved directly in `UI_UX.md` §1, which no longer depends on the removed file | 0.5 day |
| TD-013 | ~~`API_CONTRACT.md` is stale — lists Journal/Portfolio/News/AI Insights as "Not built," contradicting the actual schema and completed Module 7~~ **RESOLVED Milestone 1** | 🟡 | Never updated after Modules 6–7 landed | Anyone trusting this file gets an inaccurate picture of what exists | **Done:** founder confirmed `API_CONTRACT.md` was intentionally removed from the repository. `CURRENT_STATE.md` is the sole current-state source going forward; any rule it stated that's still worth keeping (e.g. the "React never screens/ranks/filters" data-flow discipline) has been moved into `ENGINEERING_GUIDE.md` directly | 0.5–1 day |
| TD-014 | No migration tooling; schema changes are hand-appended `alter table ... add column if not exists` statements in one growing `schema.sql` | 🟢 | Reasonable at current table/change count | Will become unreadable and risky to reason about once schema changes are more frequent | Introduce a lightweight migration tool (e.g., numbered `.sql` files applied in order) once schema changes happen more than a few times a quarter — not urgent yet | 1 day when triggered |

| TD-015 | Widespread CRLF line endings and unformatted code across most of the frontend (`npm run lint` reports ~8,249 `prettier/prettier` findings as of Milestone 2, up from ~7,720 at Milestone 1 — new Milestone 2 files were authored CRLF to match the existing convention, growing the count further) | 🟢 | Files were likely authored/edited on Windows or by a tool that doesn't normalize line endings; no `.editorconfig` or `.gitattributes` enforces LF | Cosmetic only — confirmed zero effect on build or typecheck (`npm run build` and `tsc --noEmit` both pass clean on Milestone 2's changes too) — but it means `npm run lint` has likely never passed cleanly, so real lint regressions could hide in the noise | Add `.gitattributes` (`* text=auto eol=lf`) and an `.editorconfig`, then run `prettier --write .` once as its own isolated, reviewed change — don't bundle with unrelated feature work | 0.5–1 day, but touches nearly every file, so do it alone |
| TD-016 | Leftover scaffold product name "Quant" still appears in `shared/components/layout/AppShell.tsx` (nav logo), in per-route `<title>` tags in `screener.tsx`, `journal.tsx`, `ideas.tsx`, `research.tsx`, `research.$symbol.tsx`, and `index.tsx`, and (newly noticed in Milestone 2) in the `db/schema.sql` header comment ("Quant Terminal — core schema") | 🟢 | Same root cause as the `__root.tsx` instance fixed in Milestone 1 (NEW-03) — discovered after that fix's scope was already approved for one file only | Purely cosmetic user-facing inconsistency; the app now shows "Stock Finder" in the browser tab's default title but "Quant" in the nav bar and on per-page title overrides | Same one-line-per-file fix as already applied to `__root.tsx`, plus the schema.sql comment | <0.5 day |
| TD-017 | ~~`journal_reviews` table exists in the schema (thesis review/retrospective, linked to `journal_entries`) but has no backend routes or frontend UI~~ **RESOLVED Milestone 4** | 🟡 | Milestone 2 was scoped to `journal_entries` CRUD only, per founder decision | The "review reopens" date is now shown and computed, but there's no way to actually record a review when it comes due — the retrospective/learning half of the journal's value proposition is still unbuilt | **Done:** built `routes/journal_reviews.py` + `services/journal_review_service.py` + `schemas/journal_review.py` (full CRUD for `journal_reviews`: create/read/update/delete, validation, 400/404 handling, `entryId` FK check against `journal_entries`). `entryId`/`reviewedAt` are immutable after creation by design — see `DECISIONS.md` ADR-014. `ai_comparison_summary` is a plain writable field with no auto-generation logic — see `DECISIONS.md` ADR-013. Frontend: new `features/journal/api/journalReviews.ts`, `hooks/useJournalReviews.ts`, `components/JournalReviewForm.tsx` (react-hook-form + zod, radio-group outcome/would-buy-again pickers), `components/JournalReviewList.tsx` (per-entry review timeline with add/edit/delete); `routes/journal.tsx` now prefetches and renders reviews grouped under their entry | 2–3 days |
| TD-018 | New `POST`/`PUT`/`DELETE` `/journal-entries` endpoints have no automated tests (same root cause as TD-005, now applies to a write path for the first time) | 🟡 | TD-005 (no test suite anywhere) was already open; Milestone 2 added the backend's first write path without adding tests, since TD-005 itself was out of scope | A regression in validation, the symbol-FK check, or the `reviewDueAt` auto-set logic would only be caught manually | When TD-005 is picked up, prioritize `journal_service.py` — it's now the only service layer with side effects (insert/update/delete), so it has the most to lose from an untested regression | Covered by TD-005's existing estimate |
| TD-019 | New `POST`/`PUT`/`PATCH`/`DELETE` `/pipeline-items` endpoints have no automated tests (same root cause as TD-005/TD-018, now a second write path) | 🟡 | TD-005 was already open; Milestone 3 added `pipeline_service.py` (create/update/move/delete) without adding tests, since TD-005 itself remains out of scope | A regression in the symbol-FK check, stage validation, or the stage-move `updated_at` bump would only be caught manually | When TD-005 is picked up, `pipeline_service.py` and `journal_service.py` are now the two service layers with side effects — prioritize both together | Covered by TD-005's existing estimate |
| TD-020 | ~~This Milestone 3 session could not run `npm install`/`npm run build`/`tsc --noEmit` or import the FastAPI app~~ **PARTIALLY RESOLVED Milestone 4 — network access was available this session** | 🟡 | The sandboxed environment this milestone was executed in had outbound network access disabled, unlike Milestone 2's environment | The usual "build/typecheck/import all pass" verification step from prior milestones could not be performed; a class of bugs that only a real compile/type-check would catch could be present and undetected | **Done this session:** `pip install`'d the real backend deps (`fastapi`, `sqlalchemy`, `psycopg2-binary`, `python-dotenv`, `feedparser`) and confirmed a full `app.py` import + `app.openapi()` generation succeeds, with `/journal-reviews` and `/journal-reviews/{id}` registered under the correct methods and every pre-existing route unaffected. `npm install` succeeded; `npx tsc --noEmit` and `npm run build` (full SSR build, `cloudflare-module` preset) both ran for real. **Not fully closed:** no live Postgres instance was available, so SQL was still verified only by static review against `db/schema.sql`, not an end-to-end write/read/delete cycle — see the new TD-021 below. This resolves the *tooling-access* half of TD-020 for good (network access is no longer the blocker it was in Milestone 3); whether a future sandboxed session has network access again is not guaranteed, so the note about running these checks before deploying stays relevant as a general practice, just no longer as an excuse | <0.5 day once network access is available — **done** |
| TD-021 | `journal_reviews` CRUD (TD-017) was never executed against a live database — verified by static review against `db/schema.sql`'s column list and a real FastAPI/OpenAPI import, not an end-to-end write/read/delete cycle | 🟡 | Same root cause as Milestone 2/3's identical limitation (no `DATABASE_URL`/Postgres instance available in this sandboxed environment) — the SQL itself (`insert into journal_reviews ...`, the `entry_id` FK check) has not been run against real data | If there's a subtle SQL error (column name typo, type mismatch) it would only surface on first real use, not in this session's verification | Founder should do one manual smoke test (create a review for an existing entry → edit it → delete it → refresh) against a real database before relying on this | <0.5 day |
| TD-022 | `npx tsc --noEmit`, run for real this session (see TD-020), surfaces 8 pre-existing type errors in total; 7 of them are newly attributed here — 2 in `features/journal/api/mock/journal.data.ts`, 5 in `features/market/api/mock/market.data.ts` — stale mock fixtures that no longer match the real `JournalEntry`/`PipelineItem` types (the 8th error, in `src/lib/api/companies.ts`, is already covered by TD-002, since that file is part of the known dead tree). The two mock files live inside the *live* `features/` tree (unlike TD-002's dead trees, which are the top-level `src/components/`, `src/hooks/`, `src/lib/`), but are reachable only from the dead-tree files `src/lib/api/journal.ts` and `src/lib/api/market.ts` — confirmed zero imports from any file under `routes/` or `features/` (same live-import check method as TD-002) | 🟢 | Left behind when `journal.ts`/`market.ts` were rewritten to call the real API in Milestones 2/3 — the mock fixture files themselves were never deleted, only stopped being the live data source | Cosmetic only — `npm run build` succeeds despite these errors (they're unreachable from any bundled entrypoint), confirmed this session — but `npx tsc --noEmit` cannot report a clean pass while they exist, which could mask a real regression in future output review | Delete `features/journal/api/mock/` and `features/market/api/mock/` once confirmed still zero live imports (same as any TD-002-style cleanup) — small, mechanical, low-risk, but deliberately not done this session per the founder's instruction not to touch anything TD-002-adjacent without a separate decision | <0.5 day |
| TD-023 | `data/universe_top100.csv` (Milestone 5's Nifty 50 + Nifty Next 50 dataset) was compiled from public index-constituent references (Wikipedia's NIFTY 50 / NIFTY Next 50 pages, cross-checked against NSE's semi-annual rebalance cadence) and spot-checked for a couple of tickers (`TMPV.NS` post the Oct 2025 Tata Motors demerger, `ENRIN.NS` for Siemens Energy India) — but all 100 `yahoo_ticker` values were not individually round-tripped against a live `yfinance` call in this session (no outbound network access to Yahoo's endpoint from the sandboxed environment) | 🟡 | Same root cause as TD-020/TD-021 — sandboxed session, no access to the actual external service being integrated against | If any ticker is subtly wrong (delisted, renamed, wrong exchange suffix), `fetch_prices.py` will silently skip it (it already catches and logs per-ticker failures, doesn't crash the run) — appears in the run summary's "Failed" count | Founder should run `python -m ingest.fetch_prices` once for real and check the printed summary; any symbol in the failed list needs its `yahoo_ticker` corrected in the CSV | <0.5 day to check, more only if failures are found |
| TD-024 | Milestone 5's `ingest/reset_market_data.py` (fresh-DB-init script) and `fetch_prices.py`'s new incremental-fetch logic were verified by static SQL review, syntax/import checks, and (for the pure Python logic) manual trace-through — not against a live Postgres instance. A local Postgres install was attempted in this session specifically to close this gap but failed (the `postgresql-16` server package 404'd from the `security.ubuntu.com` mirror available to the sandbox) | 🟡 | Same root cause as TD-021 — no `DATABASE_URL`/live Postgres reachable from this sandboxed environment | If there's a subtle SQL error (the `bindparam(..., expanding=True)` usage, the FK-safety check's join logic, the `truncate` statements) it would only surface on first real run | Founder should run `python -m ingest.reset_market_data --dry-run` first, read the printed report carefully (especially the "KEEPING ... because journal_entries/pipeline_items still reference them" line, if it appears), then run for real | <0.5 day, assuming no surprises |
| TD-025 | `data/universe_top100.csv`'s `index_membership` column reflects Nifty 50 / Nifty Next 50 constituents as of the index provider's most recent published rebalance (Nifty 50: 8 Dec 2025; Nifty Next 50: 30 Sep 2025) — NSE reconstitutes both indices semi-annually, so this file will drift out of date on its own schedule regardless of code changes | 🟢 | Index membership is external, human, periodic data — not something the ingest pipeline itself can detect or correct | A stale CSV means the tracked universe slowly diverges from the *actual* current Nifty 50/Next 50 without any error being raised — silent, not loud | No fix needed today; put a recurring reminder (e.g. a comment at the top of the CSV, or a calendar note) to re-diff the file against NSE's published list after each semi-annual reconstitution (typically effective late March/September) | <0.5 day per refresh, twice a year |
| TD-026 | `config/sectors.py`'s keyword-matching table (used by the Module 7 news/sector classifier) still only has entries tuned for the original 8-company universe's finer-grained sector labels (e.g. "Private Banks", "Specialty Chemicals") — it was not extended for the ~90 new companies' broader NSE sector labels introduced by Milestone 5's `universe_top100.csv` (e.g. "Financial Services", "Chemicals") | 🟢 | Out of scope for this milestone — Module 7 itself is still unvalidated against live data (TD-008), so extending its keyword table has no user-visible effect yet | None today; would matter once TD-008 is resolved and news classification is actually driving live behavior for the full 100-company universe | Extend `config/sectors.py`'s keyword table (or switch its lookup to match on `companies.sector` values directly instead of hand-maintained keyword lists) as part of whatever milestone finally resolves TD-008 | 0.5–1 day, bundle with TD-008 |

---

## Priority for next sprint (cross-reference with `ENGINEERING_ROADMAP.md`)

TD-012 and TD-013 were resolved in Milestone 1. TD-003 was resolved in Milestone 2 (journal write path). TD-004 was resolved in Milestone 3 (pipeline write path). TD-002 was resolved in Milestone 1, re-resolved in Milestone 2, and found present a third time in Milestone 3 — **reopened rather than re-resolved this time**, see that row; left untouched again in Milestone 4 at the founder's explicit instruction, pending a separate process investigation. TD-017/TD-018 were logged in Milestone 2; TD-019/TD-020 were logged in Milestone 3. TD-017 was resolved in Milestone 4 (journal reviews write path); TD-020 was partially resolved in the same session (network/tooling access, see that row); TD-021/TD-022 were newly logged in Milestone 4. TD-023 through TD-026 were newly logged in Milestone 5 (universe expansion).

1. TD-002's root cause (why the dead trees keep coming back) — not itself a feature milestone, but worth a founder decision on the delivery/handoff process before it happens a fourth time. **New data point from Milestone 4:** if each session's zip is generated from a stale snapshot rather than the live post-milestone repo, that alone would explain the recurrence without any code-level bug — worth checking how the zip is produced before investigating the code further.
2. TD-001 (fundamentals staleness visibility, short-term fix only) — cheap, prevents silently trusting bad data.
3. TD-023 and TD-024 (verify Milestone 5's new universe CSV and reset/incremental-fetch scripts against a live `yfinance`/Postgres run at least once) — the two remaining verification gaps from Milestone 5, cheap to close, should happen before relying on either script for real.
4. TD-021 (run journal_reviews' write path against a real database at least once) — the one remaining verification gap from Milestone 4, cheap to close.
5. TD-005 (test suite, now covering TD-018/TD-019's write-path gaps and — once picked up — `journal_review_service.py` too).
6. TD-022 (delete the two orphaned mock-data files found by this session's real `tsc` run) — small, mechanical, but deliberately deferred pending a TD-002-style decision rather than folded into a feature milestone.
7. Everything else, sequenced per `ENGINEERING_ROADMAP.md`.
